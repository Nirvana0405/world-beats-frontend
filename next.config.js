// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   appDir: true, // ← ❌ コメントアウト or 削除！
  // },
};

module.exports = nextConfig;
